# Laya project dependencies (CPU build of torch).
#
#   uv venv --python 3.12 .venv
#   uv pip install --python .venv/bin/python -r requirements.txt
#
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu
laya==0.3.6
fastapi==0.141.1
uvicorn==0.53.0
